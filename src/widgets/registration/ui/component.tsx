import { FC } from 'react';
import { RegistrationForm } from 'features/registration';

const Registration: FC = () => {
  return (
    <div>
      <RegistrationForm />
    </div>
  );
};

export { Registration };
